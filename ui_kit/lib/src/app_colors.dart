import 'package:flutter/material.dart';

abstract interface class AppColors {
  static const primary = Color(0xFF3861FB);
  static const primaryDark = Color(0xFF171925);
  static const accent = Colors.white;
  static const accentDark = Colors.white;
  static const accent1 = Colors.green;
  static const accent1Dark = Colors.green;
  static const accent2 = Colors.red;
  static const accent2Dark = Colors.red;
}
